# Database Connection Configuration
# Copy this file to .env and fill in your values

# Cloud SQL instance connection
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=crm_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# For Cloud SQL Auth Proxy, use:
# DB_HOST=127.0.0.1
# DB_PORT=5432

# For direct public IP connection, use the instance's public IP:
# DB_HOST=<CLOUD_SQL_PUBLIC_IP>
# DB_PORT=5432

# GCP Project: rnd-dev-asheesh
# Instance: crm-instance
# Connection name: rnd-dev-asheesh:us-central1:crm-instance
